package me.huntifi.castlesiege.flags.captureareas;

/**
 * A rectangle shaped capture area for a flag
 * The origin is to the bottom south-west of the rectangle (lowest x,y,z values in the rectangle)
 */
public class RectangleCaptureArea extends CaptureArea {
    public int width;
    public int length;
    public int height;
}
